import {StyleSheet} from 'react-native';
import {android} from '../breakpoints';

export const style = StyleSheet.create({
  container: {
    flex: 1,
    // paddingVertical: android ? 35 : 0,
  },
});
